alias \
    ta="tmux attach" \
    tl="tmux list-sessions" \
    tn="tmux new-session -s" \
    ls="eza --icons --group-directories-first" \
    lst="ls --sort=modified" \
    ll="ls -l" \
    llt="ll --sort=modified" \
    dotfiles="git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME" \

alias \
    cp="cp -iv" \
	mv="mv -iv" \
	rm="rm -vI" \
	mkd="mkdir -pv" \
